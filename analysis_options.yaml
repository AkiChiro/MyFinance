include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - "myfinance/**"   # stale snapshot directory; real source lives at root lib/

linter:
  rules:
    prefer_const_constructors: true
    avoid_print: false
